query Me {
  me(namespace: "Me") {
    status
    message
    code
    namespace
    data {
      user {
        id
        username
        email
      }
    }
    errors {
      path
      message
    }
  }
}
