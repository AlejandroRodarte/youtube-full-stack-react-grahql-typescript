mutation Register($registerData: RegisterUserInput!) {
  register(data: $registerData) {
    status
    message
    code
    _kind
    data {
      newUser {
        id
        createdAt
        updatedAt
        username
      }
    }
    errors {
      path
      type
      label
      message
    }
  }
}
