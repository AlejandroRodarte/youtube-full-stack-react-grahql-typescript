mutation Login($loginData: LoginUserInput!) {
  login(data: $loginData) {
    status
    message
    code
    _kind
    data {
      user {
        id
        createdAt
        updatedAt
        username
      }
    }
    errors {
      path
      type
      label
      message
    }
  }
}
