mutation Login($loginData: LoginUserInput!) {
  login(data: $loginData) {
    status
    message
    code
    _kind
    data {
      user {
        id
        username
        email
      }
    }
    errors {
      path
      message
    }
  }
}
