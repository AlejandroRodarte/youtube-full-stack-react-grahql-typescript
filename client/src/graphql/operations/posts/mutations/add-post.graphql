mutation AddPost($addPostData: AddPostInput!) {
  addPost(data: $addPostData) {
    status
    message
    code
    _kind
    data {
      newPost {
        id
        createdAt
        updatedAt
        title
        textSnippet
        points
      }
    }
    errors {
      path
      message
    }
  }
}