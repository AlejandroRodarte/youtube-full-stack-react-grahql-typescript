query Posts($postsData: PostsInput!) {
  posts(data: $postsData) {
    status
    message
    code
    _kind
    data {
      posts {
        id
        createdAt
        updatedAt
        title
        text
        points
        originalPosterId
      }
    }
    errors {
      path
      message
    }
  }
}
