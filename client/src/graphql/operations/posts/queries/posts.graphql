query Posts($postsData: PostsInput!) {
  posts(data: $postsData) {
    status
    message
    code
    _kind
    data {
      posts {
        id
        createdAt
        updatedAt
        title
        textSnippet
        points
      }
    }
    errors {
      path
      message
    }
  }
}
