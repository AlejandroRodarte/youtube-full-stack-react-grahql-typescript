import getUrqlClientForServerSideProps from './get-urql-client-for-server-side-props'
import common from './common'

const server = {
  getUrqlClientForServerSideProps,
  common
}

export default server
