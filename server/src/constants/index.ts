import graphql from './graphql'
import redis from './redis'
import * as MailConstants from './mail'
import * as ExpressSessionConstants from './express-session'
import regex from './regex'

const constants = {
  graphql,
  redis,
  MailConstants,
  ExpressSessionConstants,
  regex
}

export default constants
